<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.stonepaperscissors">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FinalResult1"></activity>
        <activity android:name=".FinalResult" />
        <activity android:name=".Result1" />
        <activity android:name=".Rounds1" />
        <activity android:name=".Player1" />
        <activity android:name=".Result" />
        <activity android:name=".Rounds2" />
        <activity android:name=".Rounds" />
        <activity android:name=".Player2" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>